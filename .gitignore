# Build output (includes downloaded MQTT client under build/deps/)
/build/
/dist/

# dpkg-buildpackage / debhelper (artefacts in repo root's parent are not ignored here)
/packaging/debian/hambridge/
/packaging/debian/.debhelper/
/packaging/debian/debhelper-build-stamp
/packaging/debian/files
/packaging/debian/substvars
/packaging/debian/*.substvars
/packaging/debian/*.debhelper.log

/bin/
hambridge

# FPC artefacts
*.o
*.ppu
*.a
*.or
*.rsj
*.compiled

# Optional IDE / editor project metadata (not required for `fpc` + `make` builds).
# Note: .lpr files ARE committed -- they are the Pascal program source.
*.lpi
*.lps
*.lpk
*.res
*.ico
backup/
lib/

# Local config (commit only the .example variants)
/hambridge.yaml
/hambridge.yml
/mappings/visca.yaml
/mappings/visca.yml
/config/hambridge.yaml
/config/hambridge.yml
/config/mappings/visca.yaml
/config/mappings/visca.yml

# Editor / OS noise
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
